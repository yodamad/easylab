apiVersion: kustomize.config.k8s.io/v1beta1
kind: Kustomization

metadata:
  name: lab-as-code

resources:
  - namespace.yaml
  - pvc.yaml
  - configmap.yaml
  - secret.yaml
  - deployment.yaml
  - service.yaml
  - ingress.yaml

images:
  - name: lab-as-code
    newTag: latest  # Override with actual tag when deploying

# You can add patches here for environment-specific customizations
# patches:
#   - target:
#       kind: Ingress
#       name: lab-as-code-ingress
#     patch: |-
#       - op: replace
#         path: /spec/rules/0/host
#         value: your-domain.com
